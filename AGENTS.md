# 项目协作约定

## 修改现有项目

- 修改前先阅读相关实现，不重建或回滚已有功能。
- 保留用户的未提交修改，不清理、覆盖或还原无关改动。
- Vue 组件使用 Vue SFC、`<script setup lang="ts">` 和 Composition API；不使用 React/JSX，不恢复 md-editor-v3。

## 验证方式

- 默认只修改代码，由用户手动验证。不要在每次修改后自动运行测试、打开浏览器、操作界面、启动开发服务或进行截图验证。
- 阅读代码、定位问题和审阅本次改动仍属于正常开发步骤，不受上述限制。
- 仅在用户明确要求验证，或确有特别必要（例如无法通过代码检查定位的问题、涉及数据安全的高风险改动）时，才执行最小范围的针对性验证；执行前简要说明原因。
- 不执行 `npm build`、`npm run build`、`npm typecheck`、`npm run typecheck` 或正式 Tauri 打包，除非用户明确修改这一约定。
- 完成后简要说明改动及需要用户手动检查的重点；未执行验证时如实说明，不声称测试通过。
